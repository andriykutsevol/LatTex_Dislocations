%\documentclass[a4paper,11pt,twocolumn,oneside]{book}
%
%\include{head}
%
%\begin{document}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Кристаллы с учетом микроструктуры.}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Идеальные кристаллы.}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Выберем один из узлов кристаллической решетки за начало координат. Тогда в силу ее периодичности остальные узлы можно перенумеровать с промощью целочисленного вектора $n=(n_{1}, n_{2}, n_{3})$
$$ r_{n}\equiv r(n)=\sum^{3}_{\alpha=1} n_{\alpha}a{\alpha} $$
Для простой решетки нумерация узлов совпадает с нумерацией атомов. Существование кристаллического состояния означает, что в широком инервале температур относительные смещения атомов малы по сравнению с постоянной решетки $a$(под постоянной решетки мы будем понимать величину, порядок которой совпадает с величинами основных трансляционных векторов $ \vec{a_{\alpha}} $. Поэтому сначала рассмотрим потенциальную энергию кристалла, атомы которого слегка смещены из равновесных положений, и выразим ее через смещения атомов $u(n)$. Учтем, что электроны столь подвижны, что в основном успевают приспосабливатся к движению ядер - адиабатическое приближение. В этом случае считается что потенциальная энергия кристалла $U$ функция только координат атомных ядер, фактически совпадающих с координатами центров тяжести атомов. Воспользовавшись стандартной теорией малых колебаний представим функцию $U$ в виде
$$
U=U_{0}+\frac{1}{2}\sum_{n,n^{'}}\beta^{ik}(n,n^{i})u^{i}(n)u^{k}(n^{'})
$$

Для сокращения записи опускать координатные индексы $i$, $k$, $l$, ... у матричных и векторных величин: $\textbf{B}=\beta^{ik}$, $i$, $k$=1,2,3. Тогда:

$$
U=U_{0}+\frac{1}{2}\sum\textbf{B}(\vec n,\vec n^{'})u(\vec n)u(\vec n^{'})
$$

Коэффициенты в линейном члене разложения должны быть равны нулю(мы находимся вблизи равновесия). Для безграничной однородной решетки, в силу однородности коэффициенты матрицы $\textbf{B}$ должны зависеть только от разности $(n-n^{'})$
$$
\textbf{B}(\vec{n},\vec{n^{'}})=\textbf{A}(\vec{n}-\vec n^{'}),
$$
Прямой подстановкой можно убедится, что это функциональное уравнение имеет решение $\textbf{A}=\alpha^{ik}$, $i$, $k$=1,2,3

Тогда:

$$
U=U_{0}+\frac{1}{2}\sum\textbf{A}(\vec n-\vec n^{'})u(\vec n)u(\vec n^{'})
$$
Используя лагранжев формализм классической механики запишем уравнение движения в символической форме(подразумевается уравнение движения для каждого атом в кристлле):
$$
m\ddot u(\vec{n})=-\sum_{n^{'}}\textbf{A}(\vec n-\vec n^{'})u(\vec n^{'})
$$

Предположим, что кристалл сместился как целое: $ u(n)=u_{0}=const$. Тогда внутреннее состояние кристлла изменится не может(внешние силы отсутствуют); следовательно, на атомы не будут действовать никакие дополнительные силы:
$$
\sum_{\vec n^{'}}A(\vec n-\vec n^{'})\equiv\sum_{\vec n^{'}}\alpha^{ik}(\vec n-\vec n^{'})=\sum\alpha^{ik}(\vec n)=0 
$$
а занчит добавление слагаемого 
$$
\sum_{n^{'}}\textbf{A}(\vec n-\vec n^{'})u(\vec n)
$$
в правую часть уравнения движения должно оставить его инвариантным
$$
m\ddot u(\vec{n})=-\sum_{n^{'}}\textbf{A}(\vec n-\vec n^{'})u(\vec n^{'})=
$$
$$
 =\sum_{n^{'}}\textbf{A}(\vec n-\vec n^{'})[u(\vec n)-u(\vec n^{'})]
$$

что демонстрирует инвариантность состояния кристалла относительно его смещения как целого.

Интересуясь гармоническими колебаниями представим зависимость смещения всех томов от времени в виде:
$$
u(\vec{n} ,t)=exp[-i\omega t]u(\vec{n})
$$
Подставим это в ур-е движения, полсле сокращения на $exp[-i\omega t]$ получим:
$$
m\omega^{2}u(\vec{n})=\sum_{n^{'}}A(\vec{n}-\vec{n^{'}})u(\vec{n^{'}})
$$
Введем обозначение $\varepsilon=\omega^{2}$ и перепишем это уравнение
$$
\frac{1}{m}\sum_{n^{'}}A(\vec{n}-\vec{n^{'}})u()-\varepsilon u(\vec{n})=0
$$
Решение этого уравнения можно искать в виде
$$
u(\vec{n})=uexp[i\vec{k}\vec{r}(\vec{n})].
$$
Вектор $\vec{k}$ играет роль волнового вектора колебаний кристалла. Сейчас он является свободным параметром, характретизующим искомое решение. Подствляя его получаем для смещений $u$ систему линейных однородных уравнений
$$
m\omega^{2}u=\sum_{n^{'}}A(\vec{n}-\vec n^{'})ue^{i\vec{k}[\vec{r}(\vec n^{'})-\vec{r}(\vec{n})]}
$$
Но из определения (1.1) в силу линейности следует
$$
\vec r(\vec n)-\vec r(\vec n^{'})=\sum^{3}_{\alpha =1}(n_{\alpha}-n^{'}_{\alpha})\vec{a}_{\alpha}=\vec r(\vec n-\vec n^{'})
$$
И воспользовавшись преобразованием фурье
$$
\textbf{A}(\vec k)=\sum_{\vec n}\textbf{A}(\vec n)e^{-i\vec k \vec r(\vec n)}
$$
получим
$$
m\omega^{2}u-\textbf{A}(\vec{k})u=0
$$
В данном случае $\textbf{A}(\vec{n})$ силовая динамическая матрица в узельном представлении, а $\textbf{A}(\vec{k})$ та же матрица в $\vec{k}$ представлении.
Сдесь следует отметить, что приведенный выше переход к $\vec{k}$- представлению в многом и обуславливает использование в физике твердого тела так называемого $\vec{k}$-пространства(обратного пространства). Этот переход также называют преходом к коллективным координатам, так как в случае $\vec{k}$-представления мы выделяем целое множество атомов колебания которых соответствуют данному волновому вектору $\vec{k}$. 

Условие совместноси данной системы имеет вид
$$
Det\Vert m\omega^{2}\textbf{I}-\textbf{A}(\vec{k})\Vert=0
$$
где $\textbf{I}$ - единичная матрица.
Вводя для упрощения обозначение
\begin{equation}
D(\varepsilon)=Det\Vert\varepsilon\textbf{I}-\frac{1}{m}\textbf{A}(\vec{k})\Vert
\end{equation}


перепишем условие совместности
$$
D(\omega^{2})=0
$$
Это соотношение в механике носит название характеристического уравнения для собственных частот \cite{FRG_Mexanika}, и его решение связывает частоту возможных колебаний кристалла с квазиволнвым вектором $\vec{k}$. Зависимость частоты от волнового вектора, как уже упоминалось, называется законом диспервии, а само характеристическое уравнение - дисперсионным уравнением. Следовательно, решая дисперсионное уравнение, мы получаем закон дисперсии
$$
\varepsilon=\omega^{2}(\vec{k})
$$
для колебаний кристаллической решетки.

В простой решетке каждый атом является центром инверсии, поэтому
$$
\textbf{A}(\vec{n})=\textbf{A}(-\vec{n})
$$
и очевидна вещественность матрицы $\textbf{A}\vec{k}$:
$$
\textbf{A}(\vec{k})=\sum \textbf{A}(\vec{n})\cos(\vec{k}\vec{r}(n))=
$$
$$
=\sum\textbf{A}(\vec{n})(\cos(\vec{k}\vec{r}(\vec{n})-1)
$$
Последняя часть равенства вытекает из свойства (1.8) силовой матрицы.

Если рассматривать цепочку атомов - скалярный случай вместо системы (1.19) имеется всего одно уравнение, и закон дисперсии может быть записан в явном виде:
$$
\omega^{2}( \vec{k} ) =
\frac{1}{m} \sum \alpha ( \vec{n} )
e^{ik \vec{r}( \vec{n} ) } = 
$$
$$
= \frac{1}{m} \sum \alpha( \vec{n} )
( \cos \vec{k} \vec{r}( \vec{n} ) - 1 ).
$$
Отсюда следует, что прежде всего, закон дисперсии определяет частоту как периодическую функцию квазиволнового вектора с периодом обратной решетки:
$$
\omega (\vec{k})=\omega (\vec{k}+\vec{B})
$$

Это - основное отличие закона дисперсии колебаний кристалла от закона дисперсии колебаний сплошной среды, так как для последнего характерна монотонная зависимость частоты от волнового вектора. Одновременно с этим проявляется отличие квазиволнового вектора $\vec{k}$ от обычного волнового вектора - лишь занчениям вектора $\vec{k}$ внутри одной элементарной ячейки обатной решетки, отвечают физически неэквивалентные состояния кристалла. Элементарная ячейка обратной решетки называется зоной Бриллюэна.

Возвраащаясь к закону диспрсии как решению дисперсионного уравнения (1.21), учтем, что попследнее является алгебраическим уравнением третьей степени относительно $ \omega^{2} $:
$$
Det \parallel m \omega^{2} \delta_{ij} - A_{ij}( \vec{k} ) \parallel = 0,
$$
$$
i,j = 1,2,3.
$$
Корни этого уравнения определяют три ветви закона дисперсии клебаний простой кристаллической решетки:
$$
\varepsilon = \omega^{2}_{ \alpha }, \hspace{1ex}
\alpha = 1,2,3.
$$
где $ \alpha $ номер ветви колебаний.

Итак, собственные колебания кристалла могут быть представленны в виде плоских волн (1.18), частоты которых связаны с квзиволновым вектором законом дисперсии (выше).

Чтобы отличать смещения разных ветвей колебаний, запишем решение (1.18) явно в векторном виде
$$
u(\vec{n},t) = e( \vec{k}, \alpha )
e^{i[ \vec{k}\vec{r}( \vec{n} ) - \omega t ]} 
$$
где $ e( \vec{k}, \alpha ) $ - единичный вектор. Этот вектор называется вектором поляризации.

Выделим временной множитель $ e^{i \omega t } $ и определим нормированные решения типа (1.28) формулой

$$
\varphi_{ \vec{k} \alpha } ( \vec{n} ) = 
\frac{1}{ \sqrt{N} } e ( \vec{k}, \alpha )
e ^{i \vec{k} \vec{r} ( \vec{n} ) }.
$$

При условии, что рассматриваются только векторы $ k $ находящиеся в одной элементарной ячейке обратной решетки ( физически неэквиваленные) функциям (1.18) присущи естественные свойста независимости
$$
\sum_{n'} \varphi_{k \alpha }^{*}( \vec{n} )
\varphi_{k' \alpha ' }^{*} ( \vec{n} ) = \delta_{ \vec{k} \vec{k'} } \delta_{ \alpha \alpha ' },
$$

Всвязи с этим колебания отвечающие этим функциям называются независимыми - нормальными модами колебаний.

В скалярной модели понятие поляризации отсутствует, и координатная зависимость нормльных  колебаний записывается в виде
$$
\varphi _{ \vec{k} } ( \vec{n} ) = \frac{1}{N}
e^{i \vec{k} \vec{r} ( \vec{n} ) },
$$
обеспечивающим условие нормировки
$$
\sum_{n} \varphi_{k}^{*}( \vec{n} )
\varphi_{k'}^{*} ( \vec{n} ) = \delta_{ \vec{k} \vec{k'} },
$$


Зависимость нормальных колебаний от узла кристаллической решетки $ \vec{n} $ была нами найдена в ходе непостредственного решения уравнений колебаний (1.17). Однако эта зависимость следует прямо из трансляционной симметрии кристалла. \textit{Если система материальных точек совершает гармонические колебания, то ее нормальные колебания преобразуюся по неприводимым представлениям группы симметрии системы}

Мы выяснили, что отдеьные колебния(1.18), являющиеся \textit{независимыми состояниями} движения всей кристалличской решетки, характеризуются различными значениям квазиволнового вектора $ \vec{k} $. При этом вопрос о возможных состояниях самого вектора $ \vec{k} $ остался открытым. Далеко не все точки внутри элементарной ячейки в $ k $ - пространстве могут соответствовать независимым стостяниям кристалла. Это следует хотябы из того, что множество всех точек в элементарной ячейке имеет мощность континуума, а множество незавсимых колебний, фактически совпадающее с множеством степеней свободы  кристаллической решетки, даже в случае бесконечного кристалла является счетным. Поэтому общее качественное исследование независимых состояний (колебаний) кристалла не может считаться полным, пока не установлен спектр возможных занчений $ \vec{k} $.

Во всем изложенном была существенна трансляционная симметрия, в связи с чем решетка считлась безграничной. Рассмотрение такой решетки затруднительно в математическом отношении, ввиду того, что приходится иметь дело с бесконечным числом атомов и собственных функций - состояний (1.18):
$$
u(\vec{n})=uexp[i\vec{k}\vec{r}(\vec{n})].
$$
Мы можем изучать такие системы лишь полагая число атомов конечным и затем переходя к пределу, когда это число устремляется к бесконечности - переход к плотности состояний. При этом нужно было бы ввести границы как некоторые реальные поверхности, на которых, наример, собственные функции обратились бы в нуль, однако в результате отражений от всех границ точные стационарные состояния такой системы изоображались бы соячими волнами.

Существует математический прием, который позволяет удовлетворительно решить проблемму подсчета состояний и при этом не вводить каких-либо физических эффектов, связанных с граничными условиями. Этот прием состоит в применении циклических граничных условий или \textit{условий Борна - Кармана} в уравнении (1.17). Таким условием является требование цикличности, сгласно которому

$$ u( \vec{r}_{n} ) = u ( \vec{r}_{n} + N_{1} \vec{a}_{1} ) = $$
$$=u ( \vec{r}_{n} + N_{2} \vec{a}_{2} ) = u ( \vec{r}_{n} + N_{3} \vec{a}_{3} )
$$
Накладывая это ограничение на (1.18) получаем
$$
u(\vec{r}_{n}) = 
ue^{ik[\vec{r}(n_{ \alpha } + N_{ \alpha }a_{ \alpha } )] } =
$$
$$
ue^{i\vec{k} \vec{r}( \vec{n}_{ \alpha } ) }
e^{i\vec{k} \vec{r}( \vec{N}_{ \alpha } a_{ \alpha } ) }
$$
Учитывая (2.1тут)
$$
e^{i \vec{k} \vec{r}( N_{ \alpha }a_{ \alpha } ) } = 1.
$$
Отсюда
$$
\vec{k} = 
\frac{2 \pi }{ N_{\alpha }a_{ \alpha } } p_{ \alpha }
$$

В кубической решетке формула (1.45) может быть значительно упрощена, елси положить
$ L_{ \alpha } = N_{ \alpha }a $ и направить оси координат по осям симметрии четвертого порядка:
$$
k_{x} = \frac{2\pi }{ L_{1} }p_{1},
k_{y} = \frac{2\pi }{ L_{2} }p_{2},
k_{z} = \frac{2\pi }{ L_{3} }p_{3}.
$$
Наконец, совокупность целых чисел $ p_{ \alpha } $ обычно берут расположенной симметрично на числовой оси:
$$
p_{ \alpha } = 0, \pm 1, \pm 2,...,
\pm \frac{N_{ \alpha + 1 }}{2}, \alpha = 1,2,3.
$$

Из (1.45) или (1.46) следует, что дискретные значения компонент вектора $ \vec{k} $ разделены интервалами
$ \Delta k \sim 1/L $, убывающими с увеличением линейных размеров кристалла. Поэтому в том случае, когда все линейные размеры кристалла являются макроскопическими, спектр значений $ \vec{k} $ может считатся квазинепренывным. Последнее свойство мы использовали при анализе закона дисперсии, рассматривая частоту как непрерыную функцию квазиволнового вектора.

В дальнейшем мы будем использовать квазинепрерывность спектра значений $ \vec{k} $, заменяя суммирование по дискретным значениям квазиволнового вектора интегрированием. Учтя соотношения (1.45) и (1.46), легко получить правило указанного перехода к интегрированию:
$$
\sum_{ \vec{k} } f( \vec{k} ) = \frac{V}{(2\pi)^{3}}
\int f( \vec{k} ) d^{3}k
$$
где интегрирование производится по объему одной элементарной ячейки в $ k $ - пространстве (или по одной зоне Бриллюэна).
Заметим, что если в формуле (1.47) положить $ f( \vec{k} ) \equiv 1$, то мы прийдем к очевидному соотношению
$$
\sum_{ \vec{k} } = \frac{V}{(2\pi)^{3}}
\int d^{3}k = \frac{V}{V_{0}} = N,
$$
сводящемуся к утверждению, что число независимых значений вектора $ \vec{k} $ в одной элементарной ячейке равно числу элементраных ячеек (числу атомов в простой решетке).

Для объяснения раяда свойств кристаллов, обусловленных колебаниями решетки иногда достаточно знать лишь распределение колебаний по частотам. Имея это ввиду, вводят понятие плотности колебаний.

Возпользуемся обозначением $ \varepsilon = \omega^{2} $. Тогда число колебаний квадраты частот которых лежат в интервале $ \varepsilon, \varepsilon + d \varepsilon $, можно записать в виде
$$
dn_{ \alpha } = N g_{ \alpha } ( \varepsilon ) d \varepsilon,
$$
где $ N $ - число элементарных ячеек в кристалле.
Функцию $ g(\varepsilon) $ мы будем нзывать \textit{плотностью колебаний}. Очевидно, имеет место формула
$$
g( \varepsilon ) = \frac{1}{N}
\sum_{ \vec{k} } \delta ( \varepsilon - \omega^{2}( \vec{k} ) ),
$$
где зависимость $ \omega^{2}( \vec{k} ) $ определяется законом дисперсии кристалла, а суммирование проводится по всем физически неэквивалентным значениям этого вектора (находящимся в одной ячейке обратной решетки или в первой зоне Бриллюэна).

Преобразуем формулу (2.13) переходя к интегрированию по 
правилу (1.47) и производя замену переменных интегрирования:
$$
g( \varepsilon ) = 
\frac{V_{0}}{ (2\pi)^{3} }
\int \delta [ \varepsilon - \omega^{2}( \vec{k} ) ] d^{3}k =  
$$
$$
= \frac{V_{0}}{ (2\pi)^{3} }
\int \delta( \varepsilon - \omega^{2} )
\frac{ d \omega^{2}dS_{ \vec{k} }  }
{ | \nabla \omega^{2}( \vec{k} ) | },
$$
где $ V_{0} $ - объем элементарной ячейки кристалла.

Рассмотрим теперь как упрощаются сами уравнения движения кристалла в случае длинноволновых колебаний.

Рассмотрим колебания с малым $ k $, т.е колебания для которых
$ ak \ll 1$. Разложим косинус в правой части (1.26), по степеням его аргумента и используем то обстоятельство, что функция $ \alpha( \vec{n} ) $ бстро убывает с ростом номер-вектора $ \vec{n} $. Тогда в основном приближении
$$
\omega^{2} (\vec{k}) = - \frac{1}{2m}
\sum_{\alpha,\beta}
( \vec{k} \vec{ a_{ \alpha } } )
( \vec{k} \vec{ a_{ \beta } } )
\sum_{ \vec{n} } \alpha( \vec{n} ) n_{ \alpha }n_{ \beta }
$$

Обозначим $ \vec{k} = \vec{k}\vec{\kappa} $, введя единичный вектор направления  обратном пространстве $ \vec{\kappa} $,
и представим (1.34) в виде
$$
\omega_{2} = s_{2}( \vec{\kappa} ) k_{2}
$$
где
$$
s^{2} ( \vec{ \kappa } ) =  -
\frac{1}{2m}
\sum_{\alpha,\beta}
( \vec{k} \vec{ a_{ \alpha } } )
( \vec{k} \vec{ a_{ \beta } } )
\sum_{ \vec{n} } \alpha( \vec{n} ) n_{ \alpha }n_{ \beta }
$$

Таким образом, при малых $ k $ мы получаем характерный для анизотропной сплошной среды линейный закон дисперсии звуковых колебаний:
$$
\omega = s( \kappa )k,
$$
где $ s $ имеет смысл фазовой скорости звука. Этот результат вполне естественнен, потому что малым $ k $ отвечают большие длины волн $ \lambda $ и условие $ ak \ll 1 $ эквивалентно требованию $ \lambda \ll a $, определяющему возможность перехода от механики кристаллической решетки к механике сплошной среды.

Вернемся к уравнению движения однородной кристаллической рештки
$$
m\ddot u(\vec{n})=-\sum_{n^{'}}\textbf{A}(\vec n-\vec n^{'})u(\vec n^{'})
$$
Запишем его в виде
$$
m\ddot{u}_{s}^{i}(\vec{r})=-\sum_{n's'}
\alpha_{ss'}^{ik} (\vec n-\vec{n}')u_{s'}^{k}(\vec{r}'),
$$
где
$$
\vec{r} = X_{s}( \vec{n} ), \hspace{1ex}
\vec{r}^{'} = X_{s'}( \vec{n}^{'} ).
$$

Учитывая что смещения слабо изменяются в пространстве разлжим функцию $ u_{s'} ( \vec{r}^{'} ) $ в ряд около точки
$ \vec{r}( x_{1}, x_{2}, x_{3} ) $, ограничившись членами второго порядка по $ X_{s}^{k}( \vec{n} ) - X_{s^{'}}^{k}( \vec{n}^{'} ) $:


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Реальные кристаллы.}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Рассмотрим как будут распространятся колебания при наличии в решетке изолированного изотоп-дефекта.В параграфе ... при условии что все атомы одинаковой массы мы описывали стационарные колебания кристалла уравнением

$$
m \ddot{u}(\vec n) - m \sum_{\vec{n}^{'}}\alpha (\vec n - \vec{n^{'}})[u(\vec{n})-u(n^{'})]=0
$$
Затем используя временную зависимость $ e^{-i\omega t} $ переходили к частотному представлению
$$
m\omega^{2}u(\vec{n})=\sum_{n^{'}}\textbf{A}(\vec{n}-\vec{n^{'}})u(\vec{n^{1}})
$$
Решение искали в виде
$$
u(\vec{n})=ue^{ik\vec{r}(\vec{n}}
$$
И переходя импульсному представлению Фурье преобразованим матрицы
$ \textbf{A} $
$$
\textbf{A}(\vec{k})=\sum_{n}\textbf{A}(\vec{n})e^{-i\vec{k}\vec{r}(\vec{n})}
$$
пришли к дисперсионному уравнению
$$
Det\Vert m\omega^{2}\textbf{I}-\textbf{A}(\vec{k})\Vert=0
$$

с законом дисперсии
$$
\omega^{2}=\omega^{2}_{0}(\vec k)\equiv=\frac{1}{m}\sum \alpha(\vec n) \cos (\vec k \vec r(\vec n))
$$

Затем в параграфе (переход к длин волн приближениою) мы этим уравнением описывали аккустические колебания для которых закон дисперсии

$$
\omega^{2}_{0}(0)\equiv=\frac{1}{m}\sum \alpha(\vec n)
$$

и возможные частоты колебаний лежат в конечном интервале $ (0, \omega_{m}) $.

Наиболее простой точечный дефект возникает в том случае, когда один из узлов кристаллической решетки занят атомом изотопа основного элемента, образующего кристалл. Так как атом изотопа отличается от основного атома только своей массой, то естественно считать, что возмущение кристалла при этом не меняет параметоров упругой связи атомов и сводится только к изменению мссы в узле, занятом изотопом. Пусть изотоп занимает узел в начале координат $ \vec{n}=0 $ обладая массой $ M $, отличной от массы основного атома. При наличии такого дефекта вместо уравнения ... мы получим систму из двух уравнений

$$
M \ddot{u(0, t)}-\sum \alpha (\vec{n^{'}}) u(\vec{n}^{'})=0, \hspace{1ex} \vec{n}=0
$$

$$
m \ddot{u}(\vec n, t)-\sum_{\vec{n}^{'}}\alpha (\vec n - \vec{n^{'}})u(\vec{n}^{'})=0, \hspace{1ex} \vec{n}\neq 0
$$

Эти уравнения можно объединить в единой записи
$$
m\ddot{u}(n,t)-
$$
$$
\sum_{n^{'}}\alpha(n - n^{'})u(n^{'})=
$$
$$
-(M-m)\ddot{u}(0,t)\delta_{n0}\equiv F(0,t) \delta_{n0}
$$
введя трехмерный символ Кронеккера $ \delta_{nn^{'}} $
В этом можно убедится прямой подстановкой.
Таким образом мы пришли к уравнению движения для невозмущенной решетки при наличии внешней силы \\$ F=-(m-M)\ddot{u}(0,t)\delta_{n0}$ (неоднродное дифференциальное уравнение).

Перейдем в этом уравнении от временного к частотному представлению представи $ u(\vec{n},t) $ и $ F(0,t) $ в виде интегралов Фурье.
$$
u(\vec{n},t) = \frac{1}{2\pi}
\int_{-\infty}^{\infty} u_{\omega^{2}}(\vec{n}) e^{-i\omega t} d \omega
$$

$$
u_{\omega^{2}}(\vec{n}) = \int_{-\infty}^{\infty} u(\vec{n},t) e^{i\omega t} dt
$$

$$
F(0,t) = \frac{1}{2\pi} \int_{-\infty}^{\infty}
F_{\omega^{2}}(0)e^{-i\omega t} d\omega
$$
$$
F_{\omega^{2}}(0) = \int_{-\infty}^{\infty}
u(0,t)e^{-i\omega t} dt
$$
Дельта функция от времени не зависит, поэтому она остается неизменной. 

Тогда () запишется в виде
$$
m\varepsilon u(\vec{n}) -
\sum_{n^{'}} \alpha (\vec{n} - \vec{n}^{'}) u_{\omega^{2}}(\vec{n}^{э}) =
$$
$$
=F_{\omega^{2}}(0) \delta_{n0} = 
-(M-m)\varepsilon u_{\omega^{2}}(0) \delta_{n0}
$$

Теперь перейдем к импульсному представлению произведя преобразование фурье над этим уравнением
$$
u_{\varepsilon}(\vec{n}) = \frac{1}{N} \sum_{\vec{k}}
u(\varepsilon, \vec{k}) e^{i\vec{k}\vec{r}(\vec n)}
$$

$$
u(\varepsilon, \vec{k}) = \sum u( \vec{n} ) u_{ \varepsilon }( \vec{n} ) e^{-i \vec{k} \vec{r}( \vec{r} ) }
$$

$$
F_{\omega^{2}}( \vec{n} ) = \frac{1}{N} \sum F ( \varepsilon, \vec{k} )
$$

$$
F ( \varepsilon, \vec{k} ) = \sum F_{ \varepsilon } ( \vec{n} ) e^{ -i \vec{k} \vec{r}( \vec{n} ) }
$$

$$
\textbf{A}( \vec{k} ) = \sum_{\vec{n}} \textbf{A} ( \vec{n} ) e^{-i \vec{r} \vec{r} ( \vec{n} ) } 
$$

$$
1 = \sum_{\vec{n}} \delta_{n0} e^{-i\vec{k} \vec{r}( \vec{n} ) } = e^{-i \vec{k} 0 }
$$
Тогда уравнение (1) запишется в виде:
$$
\varepsilon u(\varepsilon, \vec{k}) - \frac{\textbf{A}(\vec{k})}{m} u( \varepsilon, \vec{k} ) =
\frac{F( \varepsilon, \vec{k} )}{ m }
$$

$$
( \varepsilon - \frac{1}{m}\textbf{A}( \vec{k} ) ) u( \varepsilon, \vec{k} ) = \frac{F( \varepsilon, \vec{k} )}{m}
$$

отсюда

$$
u( \varepsilon, \vec{k} ) = \frac{F( \varepsilon, \vec{k})}{m}
\frac{1}{ \varepsilon - \omega^{2}( \vec{k} ) }
$$

Введем функцию
$$
G_{ \varepsilon } ( \vec{k} ) = \frac{1}{ \varepsilon - \omega^{2}( \vec{k} ) }
$$
тогда
$$
u( \varepsilon, \vec{k} ) = 
\frac{F( \varepsilon, \vec{k} )}{m} G_{ \varepsilon }
$$
Функция $ G_{ \varepsilon } $ называется функцией Грина для идеальной решетки.
Для удобства внесем константу $ 1/m $ в правую часть (2) и
введем обозначения
$$
M-m = \Delta m, \hspace{1ex} U_{0} = -\frac{\Delta m}{m}\varepsilon
$$
Тогда учитывая (5) легко видеть, что если в правой чатси оставить только $ \delta_{n0} $ то
$$
u( \varepsilon, \vec{k} ) = G_{ \varepsilon }( \vec{k} )
$$
И из (3) следует, что
$$
u( \vec{n} ) = G_{ \varepsilon }( \vec{n} ) =
\frac{1}{N} \sum_{\vec k}
\frac{e^{ i \vec{k} \vec{r}( \vec{n} ) }}{ \varepsilon - \omega^{2}( \vec{k} ) }.
$$

Введем формальное решение (2):
$$
u(\vec{n}) = U_{0} G_{ \varepsilon }( \vec{n} ) u(0)
$$
Если отказатся от скалярной модели, то при наличии изотоп-дефекта в узле $ n = 0 $ аналогичное выражение для вектора смещения любого атома кристалла будет:
$$
u^{i}( \vec{n} ) = U_{0} G_{ \varepsilon }^{ik}( \vec{n} ) u^{i}(0)
$$ 
Вернемся к склярной модели. Полагая в (12.9) $ \vec{n} = 0 $, мы обнаруживаем, что формула (12.9) непротиворечива лишь при условии
$$
1 - U_{0} G_{ \varepsilon }^{0} (0) = 0
$$
Подставляя сюда ()
$$
1 - \frac{U_{0}}{N}
\sum_{\vec{k}}
\frac{1}{ \varepsilon - \omega^{2} ( \vec{k} ) } = 0
$$
Перейдя от суммирования к интегрированию по квазиволновым векторам заменив суммирование интегрированием по частотам:
$$
1 - U_{0}
\int
\frac{ g_{0} (z) dz }{ \varepsilon - z } = 0
$$
здесь $ g_{0} (\varepsilon) $ - плотность колебаний идеальной решетки. Учитывая что частоты аккустических колебний невозмущенного кристалла лежат в интервале$ (0, \omega_{m} $, ясно, что уравнение (12.13) имеет смысл лишь для $ \varepsilon > \varepsilon_{m} = \omega_{m}^{2} $. Но тогда знаменатель в интеграле (12.13) всегда положителен, и уравнение (12.13) может обладать решением только при $ U_{0} < 0 $, т.е. для легкого изотопа $ (M < m) $. Однако в трехмерном кристалле наличие нужного знака возмущения еще не гарантирует существования решения уравнения (12.13). В последнем легко убедится, произведя графический анализ урвнения (12.13).

Введем обозначение
$$
F( \varepsilon ) = \varepsilon \int
\frac{ g_{0}( z ) dz }{ \varepsilon - z }
$$

и, учитывая определение (12.7), перепишем (12.3):
$$
F( \varepsilon ) = -\frac{m}{\Delta m}.
$$

Поскольку $ g_{0} (z) \sim \sqrt{ \varepsilon_{m} - z } $
при $ z \rightarrow \varepsilon_{m} $, то в точке $ \varepsilon = \varepsilon_{m} $ функция $ F( \varepsilon ) $ конечна и положительна $ ( F( \varepsilon_{m} ) = F_{m} > 0 ) $, а в малом интервале $ \varepsilon - \varepsilon_{m} \ll \varepsilon_{m} $ имеет отрицательную производную:
$$
F'(\varepsilon) \simeq -\varepsilon_{m} \int
\frac{g_{0} (z) dz}{( \varepsilon - z )^{2}} < 0.
$$
При $ \varepsilon \gg \varepsilon_{m} $, в силу выбранной нами нормировки плотности колебаний $ F( \varepsilon ) \approx 1 $, поэтому график функции $ F(\varepsilon) $ обычно имеет вид кривой, представленной на рис.~\ref{loc_chastoti}.

\begin{figure}[h]
%\vspace*{2.5cm}
\includegraphics{loc_chastoti}
\caption{Нахождение положения локальных чстот при разных знаках $ \Delta m. $}\label{loc_chastoti}
%Например, рис.~\ref{void} на с.~\pageref{void} ...
\end{figure}

Изобразив на том же рисунке горизонтальную прямую, соответствующую правой части (12.15), мы приходим к выводу,
что решение $ \varepsilon = \varepsilon_{l} $ существует
лишьпри условии
$ \left| \frac{\Delta m}{m} \right| F_{m} > 1  $. Если это условие выполнено, то решение уравнения  (12.13) дает дискретную частоту $ \varepsilon_{l} $, лежащую вне полосы непрерывного спектра (рис. \ref{loc_chastoti}, $ \varepsilon_{l} = \omega_{l}^{2} $ ).

Таким образом, для возникновения колебания с дискретной частотой, расположенной вблизи аккустической полосы, изотоп должен обладать массой $ M $ удовлетворяющей условию

$$ M < m \left( 1 - \frac{1}{F_{m}} \right) <m $$

Покажем, что амплитуда соответствующего коолебания действительно отлична от нуля лишь в некоторой области вблизи точечного дефекта.

Амплитуда локальных колебаний дается формулой (12.9), из которой следует, что ее координатная завсимость полностью определяется поведением функции Грина идеального кристлла. Перепишем функцию Грина в виде слудующего интеграла:
$$
G^{0}_{ \varepsilon }( \vec{n} ) = 
\frac{1}{N}
\sum_{ \vec{k} }
\frac{\exp{i \vec{k} \vec{r} ( \vec{n} ) }}{ \varepsilon - \omega_{0}^{2}( \vec{k} ) } =
$$
$$ 
= \frac{V_{0}}{(2\pi)}\int
\frac{\exp( i \vec{k} \vec{r}( \vec{n} )) d^{3}k }
{ \varepsilon_{0}^{2} ( \vec{k} ) }.
$$

Как всегда в подобных интегралах, их поведение на больших расстояниях $ (r\ll a) $ в основном определяется видом подынтегральной функции при малых значениях $ k(ak\ll 1) $.
Другими словам, основной вклад в интеграл (12.17) при больших
$ r $ дает интегрировние по области малых волновых векторов, где закон дисперсии идеального кристалла квадратичен по $ k $.
$$
\omega_{0}^{2} ( \vec{k} ) =
\omega_{1}^{2} + \gamma^{2}k^{2}, \hspace{0.5ex}
\gamma^{2} \sim ( \omega_{2}^{2} - \omega_{1}^{2} )a^{2}.
$$
Учтем этот закон дисперсии, заменив $ \varepsilon_{1} = \omega_{1}^{2} $, и выполним требуемое в (12.17) интегрирование:
$$
\int \frac{ \exp( i \vec{k} \vec{r} d_{3}k ) }
{ \varepsilon_{1} - \varepsilon + \gamma^{2}k^{2} } = 
$$
$$
=2\pi \int_{0}^{\infty}
\frac{ k^{2} dk }{\varepsilon_{1} - \varepsilon + \gamma^{2}k^{2}}
\int_{0}^{\pi} e^{(ikr \cos(\theta))} \sin(\theta)d\theta =
$$

$$
= \frac{2\pi^{2}}{\gamma^{2}r}
\exp\left( -\frac{r}{ \gamma }
\sqrt{ \omega_{1}^{2} - \omega^{2} }
\frac{r}{ \gamma } \right).
$$
Подставим полученный результат в формулу (12.17) и обозначим
$ \vec{r} = \vec{r}( \vec{n} ) $:
$$
G_{ \varepsilon }^{0}( \vec{n} )\thicksim
\frac{1}{r}
\left(
-\sqrt{ \omega_{1}^{2} - \omega^{2}} \frac{1}{ \gamma }
\right)
$$

Подставим (12.18) в формулу (12.9):
$$
u(\vec{n}) \sim \frac{u(0)}{r}
\exp( - \frac{r}{l} ),
$$
где введена характерная длна $ l $, определяющая область локализации колебаний с дискретной частотой:
$$
l = \frac
{ \gamma }
{ \sqrt{ \varepsilon_{1} - \varepsilon_{l} } } =
\frac
{ \gamma }{ \sqrt{ \omega_{1}^{2} - \omega_{l}^{2} } }.
$$

Таким образом, амплитуда локального колебания действительно быстро убывает с ростом рсстояния $ r $ и $ l $ длина на которой происходит это убывание.

Сам факт существования дискретной частоты, отделенной конечным интервалом от частот полосы непренывного спектра приводит к появлению особенностей в частотных зависимостях различных характеристик процессов рассеяния и поглощения. Соответствующие резонансные особенности появляются у амплитуд рассеяния различных частиц (например, нейтронов) на локальных дефектах.

Рассмотрм  локальные колебания точечного дефекта, который создается примесью замещения, когда место атома основного элемента занято "чужим" атомом. Такого же типа дефект создает вакансия в узле. При наличии этого типа дефекта меняются как масса соответствующего узла, так и постоянные упругой связи этого узла с соседними. Поэтому уравнение (12.8) должно быть заменено более общим (дефект расположен в узле $ \vec{n = 0} $:

$$
\varepsilon u(\vec{n}) = 
\frac{1}{m} \sum_{\vec{n^{'}}} \alpha ( \vec{n} - \vec{n'} )
u( \vec{n'} ) = 
$$
$$
= \sum_{n'} U( \vec{n}, \vec{n'} )u( \vec{n'} ) - 
\varepsilon \frac{ \Delta m}{ m } u(0) \delta_{n0}
$$

Где матрица $ U(\vec n, \vec{n'} ) $ определяет созданное дефектом возмущение силовых коэффициентов идеальной решетки
$ \alpha ( \vec{n} - \vec{n'} ) $

Если уравнение (12.21) описывает акустические колебания кристалла, то матрица возмущения должна обладать свойством,
анлогичным (4.3):
$$
\sum_{ \vec{n'} } U( \vec{n}, \vec{n'} ) = 
\sum_{ \vec{n} } U( \vec{n}, \vec{n'} ) = 0.
$$

Отыскание локальных частот у любого точечного дефекта, в принципе , не отличается от нахожденя таковых у дефекта-изотопа . Однако процедура становится очень громоздкой. В Виду ее очевидности мы не будем ее демонстрировать. 

Обратимся к анализу колебаний кристалла с одномерным (линейным) дефектом - дислокацией.
Для анализа колебаний кристлла с дислокацией сделаем ряд предположений:\\
1) малые колебания(вблизи их новых положений равновесия в присутствии дислокации), поэтому в нулвом приближении ось дслокации можно считать фиксированной.\\
2) дислокация прямолинейна и ее ось перпендикулярна плоскости симметрии кристалла.

Направим ось $ z $ вдоль лини дислокации и обозначим через $ \xi $ двумерный радиус-вектор в плоскости $ xOy $: $ \vec{r}=(\xi , z)$, $ \xi =(x, y) $

Если принять, что дислокация не влияет на величину плотности вещества вдоль своей оси, то вносимое дислокацией возмущение будет связано с локальным изменением силовой матрицы. При анализе длинноволновых колебаний кристалла $ (\lambda \gg a) $ это возмущение естественно считать состредоточенным на оси дислокации. Тогда рассматривая в скалярной модели смещение атомов кристалла как непрерывную функцию координат: $ u=u(\xi , z) $, можно представить уравнение колебаний в виде

$$
\omega^{2}u(\xi , z)-\frac{1}{m}\sum_{\vec{n}^{'}}\alpha (\vec{n}-\vec{n}^{'})u(\xi^{'}, z^{'})=
$$

$$
a^{2}\delta (\xi)\sum_{z^{'}}U(z-z^{'})u(0, z^{'})
$$

где $ \delta (\xi) $ - двумерная $ \delta $-функция ($ \delta (\xi)=\delta (x)\delta(y) $); суммирование по узлам кристаллической решетки может быть зменено интергированием:
$$
\sum_{z}...=\frac{1}{a}\int dz, \hspace{1ex} \sum_{\xi}...=
$$
$$
\frac{1}{a^{2}}\int dxdy ..., \hspace{1ex}\sum_{\vec{n}}...=\frac{1}{V_{0}}\int dV ...
$$

Функция U(z) в ... описывающая возмущение силовой матрицы, не зависит от частоты, является четной функцией от $ z $ и удовлетворяет очевидному требованию
$$
\sum_{z}U(z)=0
$$

Используем однородность кристалла вдоль направления $ Oz $ и применим одномерное преобразование Фурье относительно координаты $ z $:

$$
u(\xi , z)=\frac{a}{2\pi}\int \chi_{k}(\xi)e^{ikz}dk, 
$$
$$
\chi^{k}(\xi)=\sum_{z}u(\xi , z)e^{-ikz}
$$

Уравнение для $ \chi_{k}(\xi) $ выводится стантартнтым путем из ...:
$$
\omega^{2}\chi_{k}(\xi)-\sum_{\xi^{'}}\Lambda_{k}(\xi-\xi^{'})\chi_{k}(\xi^{'})=a^{2}\delta (\xi)U_{k}\chi_{k}(0)
$$
где
$$
\Lambda_{k}(\xi)=\frac{1}{m}\sum_{n_{z}}\alpha(\vec{n})e^{-ikan^{z}}, \hspace{1ex} U_{k}=\sum U(z)e^{-ikz}
$$

Уравнение ... является уравнением колебаний двухмерного кристалла со специфическим ''точечным'' дефектом в начале координат. Компонента $ k=k_{z} $ входит в это уравнение как параметр и определяет интенсивность локального возмущения $ U_{k} $. В случае $ ak\ll1 $ для функции $ U_{k} $ имеет место очевидное разложение вытекающее из...
$$
U_{k}=-k_{2}W_{0}, \hspace{1ex} W_{0}=\frac{1}{2}\sum U(z)z^{2}
$$

Для нахождения функции $ \chi_{k}(\xi) $ возпользуемся двухмерным разложением Фурье
$$
\chi_{k}(\xi)=\frac{a^{2}}{(2\pi)^{2}}\int \chi(\vec{\varkappa}, k)e^{i\vec{\varkappa}\xi}d^{2}\varkappa,
$$
$$
\chi(\vec{\varkappa}, k)=\sum_{\xi}\chi_{k}(\xi)e^{-i\vec{\varkappa}\xi}
$$

где $ \vec \varkappa $ - двумерный волновой вектор, $ \vec{\varkappa}=(k_{x}, k_{y}) $.

Компоненты Фурье $ \chi(\varkappa, k) $ определяются из соотношения
$$
[\omega^{2}-\omega^{2}_{0}(\vec{\varkappa},k)]\chi(\vec{\varkappa}, k)=-k^{2}W_{0}\chi_{k}(0),
$$
в котором функция $ \omega^{2}_{0}(\vec{\varkappa}, k) $ задает закон дисперсии идеального кристалла.

Предположим для дальнейшего упрощения выкладок, что ось $ Oz $ является осью симметрии четвертого, или шестого порядк. Тогда в дилинноволновом пределе
$$
\omega^{2}_{0}(\vec{\varkappa}, k)=s^{2}_{0}\varkappa^{2}+s^{2}k^{2}.
$$

Зависимость амплитуды колебаний от $ \xi $ следует непосредственно из ...

$$
\chi_{k}(\xi)=-(ak^{2})\frac{W_{0}}{(2\pi)^{2}}\chi_{k}(0)\int \frac{\cos (\vec{\varkappa}\xi)d^{2}\varkappa}{\omega^{2}-s^{2}k^{2}-s^{2}_{0}\varkappa^{2}}.
$$

Положив тут $ \xi=0 $, получим уравнение для частот колебаний, амплитуда которых описывается этой формулой
$$
1+(ak)^{2}\frac{W_{0}}{2\pi}\int^{\varkappa_{0}}_{0}\frac{\vec{\varkappa}d\kappa}{\omega^{2}-s^{2}k^{2}-s^{2}_{0}\varkappa^{2}}=0.
$$

Верхний предел интегрирования тут миеет порядок величины $ \varkappa_{0}\sim 1/a $. То обстоятельство, что предел интегрирования в этой формуле определяется лишь по порядку величины и имеет характер некоторого параметра ''образания'', связано с модельным предположением о точечном характере возмущения в уравнении (14.5). Предположение () о дельтаобразоной локализации возмещения на оси дислокации точно так же, как и закон дисперсии (), справедливо только для длинноволновых колебаий $ (a\varkappa\ll 1) $. В тоже время интегрирование в () и () должно распространятся на весь интервал непрерывного спектра частот. А поскольку подынтегральное выражение в () не обладает нужным для сходимости интеграла убыванием на бесконечности, мы вынуждены формально ввести естественный верхний предел интегрировния по квазиволновым векторам $ (a\varkappa_{0}\sim\pi $.

Упростим уравнение ():
$$
1-(a\k)^{2}\frac{W_{0}}{4\pi s^{2}_{0}}\ln \frac{s^{2}_{0}\varkappa^{2}_{0}}{s^{2}k^{2}-\omega^{2}}=0, 
$$
опустив малые члены порядка величины
$$
\frac{s^{2}k^{2}-\omega^{2}}{s^{2}_{2}\varkappa^{2}_{0}}\sim \frac{s^{2}k^{2}}{s^{2}_{0}k^{2}_{0}} \sim \frac{s^{2}k^{2}}{\omega^{2}_{D}}\sim (ak)^{2}\ll 1.
$$

Как и в случае с изолированным изотоп-дефектом, уравнение () относительно квадратов собственных частот $ \omega^{2} $ имеет решение только при определенном знаке $ W_{0} $, а именно для $ W_{0}\>0 $, причем обязательно $ \omega^{2}\>s^{2}k^{2} $. Если условие $ W_{0}\> $ выполнено, то () всегда имеет решение:
$$
\omega^{2}=s^{2}k^{2}=s^{2}k^{2}_{0}\exp\lbrace -\frac{4\pi s^{2}_{0}}{(ak)^{2}W_{0}}\rbrace
$$
Напомним, чт здесь $ W_{0}\>0 $ и $ s_{0}\varkappa_{0}\sim \omega_{D} $.

Частоты () обладают характерной для двухмерных задач экспоненциальной зависимостью от интенсивности возмущения, т.е. от величины $ U_{k} $. Существование локализованных вблизи дислокации колебаний требует определенного знака возмущения $ (W_{0}\>) $, и при нужном знаке соответствующая частота всегда отделена некоторой конечной щелью $ \delta\omega $ от начала спектра собственных колебаний кристалла:
$$
\delta \omega=sk-\omega_{l}\sim \frac{\omega^{2}_{D}}{sk}\exp\lbrace -\frac{4\pi s^{2}_{0}}{(ak)^{2}W_{0}}\rbrace.
$$

Мы рассмотрели бегущие вдоль дислокации упругие волны, существование которых обусловлено изменением упругих модулей в ядре дислокации.

Для идеального кристалла, в том числе подверженнго однородной деформции, частоты нормальных колебаний атомов кристалла можно определить как собственные значения динамической матрицы
[Моделирование на ЭВМ атомных конфигураций дефектов в металлах.]

\section{Совместное рассмотрение.}
 Декартовы координаты смещений атомов линейно выражаются через нормальные координаты, и наоборот. Преообразование унитарно. (Элементарная теория колебательной структуры...). Нормальные колеобания должны рассматриватся как квантовомеханические пременные(м борн).


%\end{document}